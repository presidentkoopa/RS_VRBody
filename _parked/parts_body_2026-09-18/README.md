# The part-assembled body, parked 2026-09-18

Owner: "all existing bodywork / menus and shit, pull out and park somewhere."

This is the COMPLETE part rig as it stood at RS_VRBody 8f3653b -- 17 slots, the
(kind, style) registry, per-part MODELDEF placement, ~200 fit numbers, the marine
and quake torso/arm/hand styles and every menu page that drove them.

It is parked, not deleted, and it still reads as the reference for how the slot rig
worked. models/ and the marine part IQMs are NOT copied here -- they are still in
models/ and unchanged.

Replaced by: one whole rigged body per character (models/marine/marine_whole.iqm),
the body brings its own hands, and the fit surface collapses to a handful of numbers.
